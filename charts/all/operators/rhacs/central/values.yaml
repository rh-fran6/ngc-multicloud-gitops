# These are used for post ACS install and for ACS integration
acs:
  integrations: true

job:
  image: image-registry.openshift-image-registry.svc:5000/openshift/cli:latest

managedclusters: managed

# global:
#   hubClusterDomain: AUTOMATIC

# clusterGroup:
#   name: AUTOMATIC